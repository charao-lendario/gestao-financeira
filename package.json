{
  "name": "gestao-financeira",
  "version": "0.1.0",
  "description": "MVP Sistema de Gestão Financeira",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/api",
    "packages/shared",
    "packages/database"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
